---
layout: post
title:  "markdown test!"
---
[14_dijkstra.pdf](https://prod-files-secure.s3.us-west-2.amazonaws.com/8a1e8c41-a9e3-40fb-bb9d-2b39e7479d71/a7375dab-721d-4f33-9e52-9185fac169cf/14_dijkstra.pdf)

⇒ 수정사항

11페이지입니다.

기존: 2와 4가 있다

변경: 2와 3이 있다

# 1. Dijstra

미리 경로를 다 계산 해 놓고 저장 해 놓기

![image.png](https://prod-files-secure.s3.us-west-2.amazonaws.com/8a1e8c41-a9e3-40fb-bb9d-2b39e7479d71/5b2e680c-be91-43c9-98cf-0a221d2b4a69/image.png)

pq의 정렬 기준은 : cost 오름차순

1→ 다음에 2,3 번 노드가 차례대로 들어가지만 

나오는건 3,2 로 나오게 됨

(누적 dist 작은 순으로)

![image.png](https://prod-files-secure.s3.us-west-2.amazonaws.com/8a1e8c41-a9e3-40fb-bb9d-2b39e7479d71/a0e374f8-c6d1-4af2-88d8-1c3dbce9bf44/image.png)

지금 현재 누적 dist는 (dist에 누적이 될것임)

pq.top()을 했을 때는 3이 나옴!!

pq{2,5}

![image.png](https://prod-files-secure.s3.us-west-2.amazonaws.com/8a1e8c41-a9e3-40fb-bb9d-2b39e7479d71/45b88eba-efa2-4fc3-a541-b3e1d179ad5e/image.png)

2,5가 있으면 2가 나옴

거리가 더 짧으니까!

이미 5의 dist가 6임

**2→5로 가는 것은 4+5=9이기 때문에 갱신을 하지 않고 5의 dist를 6으로 냅두는 것이 다익스트라**

# 2) 코드

```cpp
#define _CRT_SECURE_NO_WARNINGS
#include <iostream>
#include <vector>
#include <queue>

using namespace std;

struct Edge
{
    int num;
    int cost;
};

struct cmp
{
    bool operator()(Edge a, Edge b)
    {
        return a.cost > b.cost;
    }
};

// N: 6
// M: 7
int N, M;
// 1번부터 6번까지
int dist[7];
vector<Edge> v[7];

void dijkstra(int st)
{
    priority_queue<Edge, vector<Edge>, cmp> pq;
    pq.push({ st, 0 });
    dist[st] = 0;
    while (!pq.empty())
    {
        Edge now = pq.top();
        pq.pop();

        // 가지치기 조건
        if (dist[now.num] < now.cost)
            continue;

        for (int i = 0; i < v[now.num].size(); i++)
        {
            int nextCost = now.cost + v[now.num][i].cost;

            int tar = v[now.num][i].num;
            if (nextCost < dist[tar])
            {
                dist[tar] = nextCost;
                pq.push({ tar, nextCost });
            }
        }
    }
}

int main()
{
    freopen("sample_input.txt", "r", stdin);
    cin >> N >> M;
    int from, to, cost;
    for (int i = 0; i < M; i++)
    {
        cin >> from >> to >> cost;
        v[from].push_back({ to, cost });
        v[to].push_back({ from, cost });
    }

    // 초기 dist 세팅
    for (int i = 1; i <= N; i++)
    {
        dist[i] = 21e8;
    }

    dijkstra(1);

    return 0;
}
```

```jsx

struct Edge{
};

struct cmp{
	bool operator()(Edge a, Edge b){
	return a.cost>b.cost;
	}
}
```
